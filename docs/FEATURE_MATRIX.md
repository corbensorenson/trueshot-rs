# TrueShot Feature Matrix

Date: 2026-07-27

Legend: **Shipping**, **In Progress**, **Planned**

| Area | Capability | Status | Notes |
| --- | --- | --- | --- |
| Capture | Scan Wizard with quality scoring | Shipping | Live guidance and quality overlays in dashboard |
| Capture | Adaptive next-best-view planning | Shipping | Backend plan evolution per session |
| Capture | Live coverage heatmap + IQA alerts | Shipping | Coverage overlay + blur/parallax/exposure warnings |
| Capture | Auto-capture gating | Shipping | Auto-capture respects quality thresholds |
| Capture | WebRTC low-latency streaming | Planned | WebRTC server still a stub |
| Capture | HDR bracketing + focus stacking | In Progress | gPhoto applies and exactly reads back declared ISO/shutter/aperture/WB/target settings, downloads real camera files through sync + atomic rename, and supports authorized destination-scoped capture directly into project RAW storage. Unsupported controls fail instead of reporting success; retained Nikon hardware interruption/throughput and calibrated absolute-focus qualification remain |
| Capture | Burst capture + best-frame selection | In Progress | Capture path wired; full selection policy tuning pending |
| Capture | Explicit, deadlock-safe scan workflows | Shipping | Project load is side-effect free; file-backed and no-camera integration tests enforce bounded startup/capture behavior |
| Capture | Thread-affine webcam and 360-camera lifecycle | Shipping | Nokhwa handles stay on bounded dedicated actors with deadline, panic, shutdown, and atomic-capture tests; no unsafe thread assertions |
| Capture | Group-amortized Nikon Z9 NEF ROI decode | Shipping | One scaled preview/bbox per HDR-focus group; bounded parallel decoders fill ordered slots in one reusable contiguous `u16` arena with no per-frame crop allocation or sidecar; advertised layouts are restricted by the NEF support matrix |
| Capture | Streaming capture manifests | Shipping | Incremental atomic JSONL writer records explicit HDR/focus/burst order, one reference frame, stable content IDs and one reusable crop plan per group |
| Capture | Uncertainty-driven HDR/focus planner | In Progress | Calibrated deterministic core parses bounded camera option grids, selectively decodes clear or authenticated-seekable encrypted completed-NEF ROIs into stable spatial/CFA radiance and noise-whitened focus probes, uses the exact shutter/ISO/aperture sensor-exposure domain, assimilates repeated and fully censored evidence with posterior uncertainty, fits nonuniform diopter peaks, and cuts modeled time 82.7% versus a fixed 21-shot grid. The entitlement-gated dashboard discovers project-local reference/profile assets, restores durable sessions, explains selected utility and rejected candidates, requires physical-focus confirmation, and executes a server-owned capture/readback/durable-write/NEF-verification/encryption/checkpoint transaction. Exact numeric candidates round-trip to camera-declared strings, HTTP cancellation releases an exclusive RAII lease, and posterior state advances only after a measured RAW is retained. Calibrated absolute lens actuation/readback and retained real-camera timing/quality gates remain |
| Processing | Native HDR + focus fusion | In Progress | Tiled `f32` fusion consumes the `u16` arena directly with lazy exposure/WB calibration, optics-bound per-CFA flat-field gain and same-CFA persistent-defect repair, focus-plane scale/translation, per-bracket exposure-normalized global translation, selective compact local motion, forward/backward disocclusion rejection, median/MAD deghosting, and sparse same-CFA low-frequency illumination/high-frequency detail fusion only where calibrated disagreement requires it. Scale-decoupled UHD focus evidence, physically scaled saturated-core glare exclusion, exact correction/glare/low-source/detail-source/state/frequency/boundary maps, confidence, and depth-consistent aperture-valid refusion remain measurement-only. Apple ARM64 uses a reported deterministic NEON focus stencil and exact sort-free trimmed evidence with a scalar fallback. Mixed boundary support anchors one traceable measured plane rather than interpolating incompatible depth rays; glare suppression never changes measured radiance. Retained real optical calibration and direct Helicon/Lightroom qualification remain |
| Processing | Fusion provenance inspector | Shipping | Every project exposes an Advanced Capture-gated inspector for measured source/deghost/frequency/glare/aperture-boundary/sensor-correction/operator-revision layers, exact archival map downloads, intervention counts, policies, calibration/fallback disclosure, and timing/memory evidence. Operators can draw non-overlapping regions and bind each to one real source frame; the server emits an immutable schema-bound document, optionally encrypted without a plaintext sibling. The Inspector queues and cancels the revision locally, polls persisted progress, and refreshes the distinct immutable result. Strict replay metadata binds every processor control and project-local calibration digest; the server launches only the packaged sibling CLI without a shell through a bounded zeroized stdin envelope, retains bounded diagnostics, and re-encrypts revision artifacts when required. `TSE2` RAW and calibration profiles are authenticated and selectively read without plaintext staging; legacy `TSE1`, clipped, disoccluded, stale, overlapping, out-of-bounds, edit-chained, path-escaping, symlinked, and modified requests fail closed. A private 21-frame Z9 integration gate preserves the exact base report while emitting a distinct source-bound revision and exact operator map. Bounded server manifests reject unsafe, malformed, generative, or non-archival reports. Physically constrained glare/trimap controls remain |
| Processing | Deterministic focus/HDR quality gates | In Progress | Scale-decoupled synthetic baseline is 44.161 dB PSNR and 100% depth accuracy with a 44 dB floor plus radiance/deghost/CFA/visibility/local-motion/disocclusion/source-fallback/glare/trimap invariants; synthetic crossing-halo energy falls from 23.759999 to 0.002434 against the 50% improvement floor. Adversarial frequency-separated fusion reduces error from 0.03030242 to 0.00000406 while remaining inside the measured CFA envelope; its static fast path and tiled output are exact. Conditional censored inference reaches 96.203% coverage at nominal 95%, 0.005563 mean bias, and 0% false conflicts over 28,021 clipping events; ordinary posterior coverage is 96.444%. Five source-bound Apple M1 production runs on a private 21-frame native-resolution Z9 ROI pass 8 s wall-p95, 384 MiB physical-footprint, 832 MiB RSS, 100 J, fair-thermal, Metal-only, measured-only, and exact 11-artifact/semantic-provenance gates; observed wall p50/p95 is 5.939/6.073 s. Redistributable calibrated real corpus, full-sensor/cross-generation Apple gates, and direct competitor gates remain |
| Processing | Calibrated probabilistic RAW fusion | In Progress | One streamed paired dark/flat pass independently fits exact-ISO Poisson-Gaussian noise and an optics-bound per-CFA spatial gain grid/persistent-defect map. Whole-pair fit/holdout splits, variance/coverage/spatial-p95 gates, probabilistic censor margins, bounded digest-bound profiles, gain-squared uncertainty propagation, same-CFA pre-interpolation repair, and exact provenance maps are implemented. Clipped evidence is integrated through a bounded analytic Poisson-Gaussian survival likelihood with heteroscedastic score/Fisher information, stable far-tail evaluation, a six-sigma conflict gate, and an allocation/iteration-free robust WLS fast path when no sample clips. Identity includes camera, sensor geometry/bit depth, lens, aperture, focal length, and measured focus envelope; mismatches fail closed. Retained real Z9 integrating-sphere and posterior qualification remain |
| Processing | Physical PSF-aware focus fusion | In Progress | Native fusion validates lens/sensor geometry and fits bounded-memory sub-plane peaks on nonuniform diopters. A digest-bound `trueshot.lens-psf.v1` profile models focus breathing, entrance pupil, and residual field PSF from source-hashed fit/holdout measurements; camera/sensor/lens/aperture/focal/focus mismatches fail closed. `extract-lens-psf` verifies retained hashes, selectively decodes bounded native-Bayer target ROIs, robust-fits an analytic disk PSF and MTF50, derives effective focal length from known target geometry, gates distance uncertainty/metadata disagreement, and publishes no measurements on quality failure. The synthetic 2.5-16 px/3-17 degree sweep has 0.005365 maximum diameter error. Calibrated image distance drives metric depth, field PSF drives confidence and crossing support, and the conservative effective pupil drives aperture-valid projection. The tri-state mixed-pixel transform anchors ambiguous support to one measured plane and exports profile/fallback provenance plus unquantized meter depth. No profile is explicitly reported as ideal-only. Retained real-lens profiles and real hair/fur/transparency halo/MTF qualification remain |
| Processing | Camera-profiled color pipeline | Planned | Current archive is untagged camera-linear RGB; Z9 camera-to-standard transform, ICC/DNG tagging, and DeltaE 2000 gates are release blockers |
| Processing | Camera-profiled RAW normalization | Shipping | Real TIFF make/model identity selects verified sensor black/saturation levels; Z9 firmware 5.00 uses measured 1008/15311 levels and explicit overrides remain available |
| Processing | CFA-exact adaptive demosaic | In Progress | AHD preserves measured RGGB samples and true black and uses a robust homogeneity margin that improves synthetic red/blue PSNR. Production Apple Metal executes the same directional/Lab/homogeneity pipeline in adapter-bounded haloed bands with stage-aware unified-memory admission, explicit provenance, and deterministic CPU fallback. HDR normalization reuses one bounded host band instead of a full-frame copy. The retained M1 release gate has exact CFA samples, 123.965 dB CPU parity, 5.29e-4 max normalized error, 77.14 MB admitted scratch, and 1.30x/1.27x p50/p95 speedup. Its mandatory 6.25x HDR stress uses exact 8x normalization, remains 1.30x/1.30x faster, and tightens max normalized error to 5.63e-5; all four speed percentiles fail closed below 1.10x. A private 21-frame Z9 run completed three Metal bands and atomic exports without fallback; redistributable chart/corpus and cross-generation Apple gates remain |
| Processing | Hierarchical Bayer-preserving super-resolution | Shipping | Native mode retains Bayer output; requested SR uses alignment diversity and joint high-resolution demosaic |
| Processing | Native FAST/BRIEF + robust geometry | Shipping | FAST-9, BRIEF, adaptive RANSAC, MAGSAC, triangulation, and regression tests run without OpenCV |
| Processing | Million-file bounded execution | Shipping | Memory-credit admission, adaptive decode workers, one-deep async export, durable retries/cancellation and crash-safe artifact-verified resume. macOS admission uses only Mach free/inactive/speculative reclaimable pages, excludes active/wired/compressed memory, and is shared by validation, progress, status, and adaptive worker control |
| Reconstruction | Photogrammetry (SfM + MVS) | Shipping | PatchMatch + fusion pipeline |
| Reconstruction | In-memory fused-image handoff | Shipping | SfM feature extraction and dense MVS share `Arc<RgbImage>` buffers; focus-stack persistence is optional |
| Reconstruction | 3D Gaussian Splatting | Shipping | Trainer + renderer with gradients |
| Reconstruction | Avatar pipeline (SMPL-X + mesh) | In Progress | SMPL-X fit + mesh reconstruction in core |
| Reconstruction | Scene reconstruction sync | Shipping | Audio sync + pose-aware confidence |
| Reconstruction | Dynamic 4D Gaussian Splatting | In Progress | Training path present; productization pending |
| Reconstruction | On-device preview + hybrid | Planned | Device preview + quality preset pipeline not productized |
| Editing | Mesh cleanup toolkit | Planned | Hole fill/decimate/smooth/normals repair pending |
| Editing | Splat editing toolkit | Planned | Brush/plane/sphere prune + density controls pending |
| Output | Floorplan + measurement export | Shipping | Area/perimeter/GeoJSON/CSV outputs |
| Exports | glTF/GLB/USD/PLY/OBJ | Shipping | Provenance embedded in exports |
| Exports | FBX/USDC/USDZ | Planned | Engine/DCC export breadth still pending |
| Sharing | Share links + viewer | Shipping | Expiring links + share viewer route |
| Sharing | Share analytics + public gallery | In Progress | Analytics present; gallery/short-links pending |
| Storage | Cloud/NAS backup + restore | Shipping | Provider-backed backups with integrity checks |
| Security | Encryption at rest | Shipping | Envelope-wrapped project keys |
| Security | Audit anchors + provenance signing | Shipping | Signed anchors and embedded metadata |
| Platform | Local-first compute boundary | Shipping | All capture/recon/render/export runs on customer hardware |
| Licensing | Modular bundles + trials | Shipping | Entitlements + trials enforced server-side |
| Licensing | License activation (key + JSON) | Shipping | Device-bound activation + seat management |
| Platform | API server + dashboard | Shipping | Actix + React stack |
| Platform | CLI workflows | Shipping | Headless processing + export |
| Platform | gRPC SDK surface | Planned | Proto crate is a stub |
| Platform | Optional Redis distributed event bus | Shipping | Bounded loop-safe relay, forced-disconnect recovery, shared calibration cache, and fail-fast local-only degradation |
| Platform | OpenAPI generation | Shipping | Generated at runtime from route annotations |
| Platform | Role-based access + SSO | Planned | User/role management still missing |
| Release | Signed installers + auto-update | Planned | Launcher signing not implemented |
| Release | SBOM/SLSA attestations | Planned | Supply chain hardening in progress |
| Release | Dashboard dependency advisory gate | In Progress | Production build and touched-surface lint pass, but the 2026-07-27 lockfile audit still reports 6 production advisories (5 high) and requires legacy peer resolution because `react-joyride` does not declare React 19 support |
| Release | Strict Rust quality gate | Shipping | Formatting, workspace tests, doctests, and strict default-feature Clippy pass for all targets |
| Release | Executed benchmark smoke gates | In Progress | Public-API benchmarks execute in CI; deterministic full E2E quality fixture remains open |
