This directory is reserved for runtime/static assets served by the server.

Do not commit frontend build artifacts here. Use the dashboard build pipeline and
deploy the built assets separately for production.
